# AI Rules

## UI/UX Guidelines
- **Use Toasts/Snackbars instead of Alerts**: For notifications and feedback (e.g., success messages, non-critical errors), avoid using blocking `alert()` dialogs. Use non-intrusive Toast or Snackbar components instead.
